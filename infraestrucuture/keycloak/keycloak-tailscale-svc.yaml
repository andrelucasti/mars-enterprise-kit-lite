apiVersion: v1
kind: Service
metadata:
  name: keycloak-tailscale
  namespace: keycloak
  annotations:
    tailscale.com/hostname: "keycloak-lab"
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: keycloak
  ports:
    - name: http
      port: 80
      targetPort: 8080
